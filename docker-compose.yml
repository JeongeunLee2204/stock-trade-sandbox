version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: stock-trade-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: stock_trade
    ports:
      - "3306:3306"